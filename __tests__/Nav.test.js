import React from 'react';
import renderer from 'react-test-renderer';
import Nav from '../components/Nav';

describe('Nav Component Tests', () => {
    describe('Nav Component Snapshot Test', () => {
        it('should not have changed its UI', () => {

            expect(1).toEqual(1);            
        });
    });
});
